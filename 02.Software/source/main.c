#include <stdio.h>
#include <stdlib.h>
#include "morse_code.h"
#include "binary_tree.h"
int main(int argc, char *argv[])
{
    
    //int *a = (int*)malloc(sizeof(int));
    printf("Hello World\r\n");
    morse_init();
    //bt_node *p = bt_new_nodes('A');

    //printf("%s\r\n", morse_encode_character('A'));
    return 0;
}